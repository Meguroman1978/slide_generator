# 🎯 あなた専用のセットアップガイド

## 📋 現在の状況

### ✅ 正しい情報
- **プロジェクトURL**: `https://script.google.com/home/projects/1f86p4jLM8nCdCLUqi_3EiCLXp_lsR4-FuumQJytHB21CoT1PZ0aFY_Kn/edit`
- **現在のデプロイURL**: `https://script.google.com/macros/s/AKfycbwYw9agkH9pig0IkksI2KDrP1rmekCKrFA0vtw8xe6K5nznEkYb1LeWRxRS9F0vkBxKXA/exec`

### ❌ 現在の問題
現在のデプロイURLをテストした結果：

```bash
curl "https://script.google.com/macros/s/AKfycbwYw9agkH9pig0IkksI2KDrP1rmekCKrFA0vtw8xe6K5nznEkYb1LeWRxRS9F0vkBxKXA/exec"

結果: 302 Redirect → Googleログインページ
```

**原因**: デプロイ設定で「アクセスできるユーザー」が「全員」になっていません。

---

## 🔧 完全な解決手順

### ステップ1: プロジェクトを開く

1. この正確なURLを開いてください:
   ```
   https://script.google.com/home/projects/1f86p4jLM8nCdCLUqi_3EiCLXp_lsR4-FuumQJytHB21CoT1PZ0aFY_Kn/edit
   ```

2. `Code.gs` が開いているか確認

### ステップ2: コードを確認

`Code.gs` に以下のコードが入っているか確認してください。もし違う場合は、このリポジトリの `apps-script/Code.gs` から正しいコードをコピーしてください。

特に重要な部分：
- `doPost(e)` 関数: POSTリクエストを処理
- `doGet(e)` 関数: GETリクエストを処理（テスト用）
- `doOptions(e)` 関数: CORSプリフライト対応
- `createJsonResponse()`: CORSヘッダー付きレスポンス

### ステップ3: 既存のデプロイを確認・削除

#### 3-1. デプロイ状態を確認

1. 上部メニューの **「デプロイ」** をクリック
2. **「デプロイを管理」** を選択

#### 3-2. 既存デプロイの設定を確認

現在のデプロイ（`AKfycbwYw9agkH9pig0IkksI2KDrP1rmekCKrFA0vtw8xe6K5nznEkYb1LeWRxRS9F0vkBxKXA`）を見つけて、設定を確認：

- **「次のユーザーとして実行」**: これは何になっていますか？
- **「アクセスできるユーザー」**: これは何になっていますか？

#### 3-3. もし「アクセスできるユーザー」が「全員」でない場合

2つのオプションがあります：

**オプションA: 既存デプロイを編集**
1. 既存デプロイの鉛筆アイコン（編集）をクリック
2. **「アクセスできるユーザー」を「全員」に変更**
3. **「デプロイを更新」** をクリック
4. URLは変わりません（同じURLが使えます）

**オプションB: 新規デプロイを作成**（推奨）
1. 既存デプロイを削除（ゴミ箱アイコン）
2. 「デプロイ」→「新しいデプロイ」へ進む

### ステップ4: 新規デプロイを作成（オプションBの場合）

1. **「デプロイ」** → **「新しいデプロイ」**

2. **種類を選択**:
   - 歯車アイコン ⚙️ をクリック
   - **「ウェブアプリ」** を選択

3. **設定を入力**:
   ```
   説明: AI Presentation Generator
   
   ⚠️ 次のユーザーとして実行: 自分（あなたのGmail）
   
   ⚠️ アクセスできるユーザー: 全員  ← これが最重要！
   ```

   **重要**: 「アクセスできるユーザー」のドロップダウンで、必ず **「全員」** を選択してください。
   
   以下は **ダメ** です：
   - ❌ 「Googleアカウントを持つすべてのユーザー」
   - ❌ 「ドメイン内のすべてのユーザー」
   - ❌ 「自分のみ」

4. **「デプロイ」** をクリック

5. **承認を求められたら**:
   - 「アクセスを承認」をクリック
   - あなたのGoogleアカウントを選択
   - 「詳細」→「（安全ではないページ）に移動」をクリック（自分のスクリプトなので安全です）
   - 「許可」をクリック

6. **新しいURLをコピー**:
   ```
   ウェブアプリ URL: https://script.google.com/macros/s/AKfycby.../exec
   ```
   このURLをコピーしてください。

### ステップ5: URLをテスト（必須！）

新しいURLを取得したら、**必ずテスト**してください。

#### 方法1: curlコマンド（推奨）

ターミナル（macOS/Linux）またはコマンドプロンプト（Windows）で：

```bash
curl -X GET "あなたの新しいURL"
```

**✅ 成功の場合（これが表示されればOK）**:
```json
{
  "status": "ok",
  "message": "AI Presentation Generator - Google Apps Script is running",
  "timestamp": "2025-11-29T12:00:00.000Z"
}
```

**❌ 失敗の場合（HTMLが表示される）**:
```html
<HTML>
<HEAD>
<TITLE>Moved Temporarily</TITLE>
...
```
→ デプロイ設定が間違っています。ステップ3に戻って「全員」を選択し直してください。

#### 方法2: ブラウザで直接開く

新しいURLをブラウザのアドレスバーに貼り付けてアクセス：

**✅ 成功**: JSONテキストが表示される
```json
{"status":"ok","message":"AI Presentation Generator - Google Apps Script is running","timestamp":"..."}
```

**❌ 失敗**: 「ページが見つかりません」またはログイン画面が表示される
→ デプロイ設定を確認

### ステップ6: アプリに新しいURLを設定

1. **アプリを開く**:
   ```
   https://3005-isc74kp5hxf1e5sjpc12t-a402f90a.sandbox.novita.ai
   ```

2. 右上の **「設定」アイコン（⚙️）** をクリック

3. **「API設定」タブ** を選択

4. **「Google Apps Script URL」** フィールドに新しいURLを貼り付け:
   ```
   https://script.google.com/macros/s/AKfycby.../exec
   ```

5. **「設定を保存」** をクリック

### ステップ7: エンドツーエンドテスト

1. **ファイルをアップロード**（PDFや画像など）
2. **資料タイプを選択**（例：営業資料）
3. **「解析を開始」** をクリック
4. 分析完了後、**ストーリーラインを選択**
5. **「ドラフト生成」** をクリック
6. 必要に応じて下書きを編集
7. **「スライド生成」** をクリック
8. 「エクスポート」タブに移動
9. **「Google Slidesで開く」** をクリック

**✅ 成功**: Google Slidesが新しいタブで開きます！

**❌ 失敗**: 401エラーまたはその他のエラーが表示される
→ ステップ5に戻ってURLが正しく動作しているか再確認

---

## 🔍 トラブルシューティング

### 問題1: URLをテストしてもHTMLが返ってくる

**原因**: 「アクセスできるユーザー」が「全員」になっていない

**解決策**:
1. プロジェクトに戻る
2. 「デプロイ」→「デプロイを管理」
3. 既存デプロイを編集
4. **「アクセスできるユーザー」を「全員」に変更**
5. 「デプロイを更新」
6. 数分待ってから再度テスト

### 問題2: 承認画面が表示されない

**原因**: すでに承認済み、またはポップアップがブロックされている

**解決策**:
- ブラウザのポップアップブロッカーを無効化
- シークレットモードで試す
- 「デプロイを管理」→既存デプロイを削除→新規デプロイで再試行

### 問題3: 「Page Not Found」エラー

**原因**: Web Appとして正しくデプロイされていない

**解決策**:
1. 「デプロイ」→「新しいデプロイ」
2. 種類で **「ウェブアプリ」** を確実に選択
3. 手順通りに再デプロイ

### 問題4: URLが変わらない

**原因**: 既存デプロイを編集した場合、URLは変わりません

**対応**: これは正常です。同じURLで動作するか再テストしてください。

---

## 📝 チェックリスト

デプロイ前に確認：

- [ ] 正しいプロジェクトURLを開いている（`1f86p4jLM8nCdCLUqi_3EiCLXp_lsR4-FuumQJytHB21CoT1PZ0aFY_Kn`）
- [ ] `Code.gs` に正しいコードが入っている
- [ ] `doPost`, `doGet`, `doOptions` 関数がある
- [ ] 既存デプロイの設定を確認した
- [ ] 種類: 「ウェブアプリ」を選択
- [ ] 次のユーザーとして実行: 「自分」
- [ ] **アクセスできるユーザー: 「全員」** ← 最重要
- [ ] 承認を完了した
- [ ] 新しいURLをコピーした

デプロイ後に確認：

- [ ] curlでURLをテストした
- [ ] JSONレスポンスが返ってきた（HTMLではない）
- [ ] アプリの設定に新しいURLを保存した
- [ ] ブラウザのキャッシュをクリアした
- [ ] エンドツーエンドテストが成功した

---

## 🎯 まとめ

### 最も重要なポイント

```
⚠️ 「アクセスできるユーザー」を「全員」に設定 ⚠️
```

これが正しく設定されていれば、401エラーは発生しません。

### テストコマンド

```bash
# このコマンドでJSONが返れば成功
curl -X GET "https://script.google.com/macros/s/AKfycby.../exec"
```

### 期待される結果

```json
{"status":"ok","message":"AI Presentation Generator - Google Apps Script is running","timestamp":"..."}
```

---

**この手順に従えば、確実に動作します！** 🎉

何か問題が発生した場合は、具体的なエラーメッセージをお知らせください。
